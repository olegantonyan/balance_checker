require 'spec_helper'

describe BalanceChecker do
  it 'has a version number' do
    expect(BalanceChecker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
